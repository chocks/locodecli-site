---
title: MCP Servers
description: Extend locode with Model Context Protocol (MCP) servers.
---

Locode supports the Model Context Protocol (MCP) for extending agent capabilities with external tools and data sources.

:::note
MCP server support in locode is under active development. This page will be updated as the feature matures.
:::

## What is MCP?

The [Model Context Protocol](https://modelcontextprotocol.io/) is an open standard that lets AI applications connect to external tools and data sources through a unified interface. MCP servers provide tools that agents can call during task execution.

## Built-in tools

Even without MCP, locode agents have access to built-in tools:

- **readFile** — read file contents from your project
- **shell** — execute shell commands (from an allow-list for safety)
- **git** — run git operations

## Configuring MCP servers

Coming soon. Check back for documentation on connecting external MCP servers to locode.
